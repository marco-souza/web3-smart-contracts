{
  "name": "@cb/contracts",
  "module": "index.ts",
  "scripts": {
    "dev": "cd ./page && bun run ./server.ts",
    "fmt": "bunx prettier --write test scripts page",
    "hh": "hardhat",
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/modules/Lock.ts",
    "local:deploy": "npx hardhat ignition deploy ./ignition/modules/Lock.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/bun": "latest",
    "hardhat": "^2.22.19"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "ethers": "^6.13.5"
  }
}
