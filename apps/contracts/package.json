{
  "name": "@cb/contracts",
  "module": "index.ts",
  "scripts": {
    "dev": "cd ./page && bun run ./server.ts",
    "hh": "hardhat",
    "compile": "hardhat compile",
    "deploy": "hardhat ignition deploy ./ignition/modules/Lock.ts",
    "local:deploy": "npx hardhat ignition deploy ./ignition/modules/Lock.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/bun": "latest",
    "hardhat": "^2.22.18"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^6.13.5"
  }
}
